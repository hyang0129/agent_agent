"""Agent evaluation integration tests — run against approved fixtures in tests/fixtures/."""

import pytest


@pytest.mark.integration
def test_placeholder() -> None:
    pytest.skip("Not yet implemented — see integration-test-fixtures.md §12")
